div.page-controller(ng-controller='TimeStatCtrl')
  div.query-filter-item
    div
      div.form-group.date-item
        div.date-item-title 开始时间
        div.input-group.date-input
          input.form-control(type='text',ng-change='submitQuery()',uib-datepicker-popup='{{format}}',ng-model='min_time',is-open='popup1.opened',ng-required='true',close-text='关闭',clear-text='清空',current-text='今天',datepicker-options='options',alt-input-formats='altInputFormats')
          span.input-group-btn
            button.btn.btn-primary(type='button',ng-click='open1()')
              i.glyphicon.glyphicon-calendar
      div.form-group.date-item
        div.date-item-title 结束时间
        div.input-group.date-input
          input.form-control(type='text',ng-change='submitQuery()',uib-datepicker-popup='{{format}}',ng-model='max_time',is-open='popup2.opened',ng-required='true',close-text='关闭',clear-text='清空',current-text='今天',datepicker-options='options',alt-input-formats='altInputFormats')
          span.input-group-btn
            button.btn.btn-primary(type='button',ng-click='open2()')
              i.glyphicon.glyphicon-calendar

    div.chart-types
      div.date-item-title 视图维度
      div.chart-type-item(ng-repeat='type in types',ng-class='active_type_index==$index?"active":""',ng-click='showActiveType(this)')
        div(ng-bind='type')

  div#chart_panel_full
    div.chart_container
      div(ng-show='!request_loading')
        div.chart-title 设备使用时长的统计图
        div
          span.start_ms(ng-bind='start_ms| date:"yyyy-MM-dd HH:mm:ss"')
          span 至
          span.end_ms(ng-bind='end_ms| date:"yyyy-MM-dd HH:mm:ss"')
      div.process(ng-show='request_loading')
        div.spinner
          div.double-bounce1
          div.double-bounce2
        div.process-title 正在查询数据，请稍候

      div#bar_chart_user(ng-show='!request_loading')
      div#timeline(ng-show='!request_loading')
